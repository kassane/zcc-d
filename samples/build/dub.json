{
    "name": "cxxbuild",
    "targetType": "executable",
    "targetPath": "build",
    "mainSourceFile": "source/app.d",
    "lflags": [
        "-L$PACKAGE_DIR/build"
    ],
    "lflags-windows": [
        "/LIBPATH:$PACKAGE_DIR\\build"
    ],
    "libs": [
        "cffi"
    ],
    "preBuildCommands-posix": [
        "$DUB --single build.d"
    ],
    "preBuildCommands-windows": [
        "powershell New-Item -ItemType Directory -Force -Path $PACKAGE_DIR/build",
        "zig cc $PACKAGE_DIR/source/ffi.cc -target native-windows-msvc -O2 -s --emit-static-lib -fPIC -o $PACKAGE_DIR/build/cffi.lib"
    ]
}
